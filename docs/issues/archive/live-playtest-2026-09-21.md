# 首轮 DeepSeek 实玩

日期：2026-09-21。局号：96bdadca-62e7-4873-a9f5-115f5b2a5ad5。完整记录仅在本地 gitignored runs/ 同名 JSON。

玩家由开发 agent 扮演，选择守卫皇宫：按玩家可见奏报，宫内官印失踪与异常通行比东门去向不明的武装更直接威胁皇帝安全。开发者已知场景设定，因此这轮是功能实玩，不是盲测。

DeepSeek 返回 obey_ruler，置信度 0.78；简短理由权衡了宫内异常、禁军职责、抗命惩罚，以及丞相的任命/资助关系。引擎在 t=60 执行驻守，t=90 向皇帝回报宫城安全。没有把模型生成的文字直接当作世界结果。

请求：官方 deepseek-flash，thinking enabled / high。一次调用约 4413ms，866 输入 token、512 输出 token（其中 393 reasoning token），总计 1378。当前 SDK 未提供可独立核验的响应模型名，returnedModel/returnedProvider 明确为 null，不冒充测得值。

浏览器复盘和回放验证通过：22 个 committed 事件，重放得到完全相同状态，无额外模型请求。开发服务重启后仍能恢复已完成结果和重放。未保存原始 reasoning，运行目录没有 Harness sessions 文件，构建产物及运行记录未发现密钥。

体验观察：模型能引用角色处境解释选择；本轮没有观察到抗命，不能由一局推断服从倾向。中文简短理由混入了一个英文 Chancellor，生成文案的语言一致性仍需改善。界面切换语言不会自动重译已生成记录。

限制：当前只有一轮、两个引擎已实现的 NPC 能力，危机客观设定固定。未完成局仍在内存，服务重启后不能恢复；完整战役、自由下令和多 provider 不在本切片内。
