# PR #2 review 取舍

2026-09-21，项目所有者确认：修实际问题，保留合理兼容，优先做任命层。原提案和详细调查保留在 [PR #2](https://github.com/unryuu/throne/pull/2)。

- BUG-0001：暂不处理。当前静态词典由类型系统保证键齐全，未确认实际缺译。
- BUG-0002：暂不处理。内部 token 人化是已有测试明确允许的显示兼容；若新增玩家文案漏译，按具体缺陷处理，不全量改翻译接口。
- BUG-0003：修复。交给丞相的合法选项没有落实效果；未知事件被静默放过。原始运行和重放仍一致，两者都漏执行，不能称为两次运行分叉。隐藏证据后持有人不变本身不足以证明错误。
- BUG-0004：修复必要字段读取。损坏数据与空数据应区分，采用共享严格读取函数，暂不为所有事件建立完整 schema 体系。
- BUG-0005：暂不处理。保留 JSON 包装兼容和结构校验；不把格式包装认定为模拟失败。更严格的响应约定留到真实模型接入时评估。
- BUG-0006：修复。保留错误原因并提供重开入口，初始化失败不能永远显示加载中。

本记录不属于日常必读。实际修复结果随对应工作项归档。
